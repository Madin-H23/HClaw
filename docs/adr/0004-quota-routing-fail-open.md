# 额度路由 fail-open：优化维度，不是准入闸门

额度感知路由的所有数据路径（quota-tool、CC Switch 库）都允许失败：数据陈旧照用（快照标注数据时间），数据源整体不可用则回退上游原生选路——效果等于「暂无额度感知」，绝不变成「拒绝服务」。唯一例外：上游 billing 的用户余额闸保持其原有 fail-closed 语义不动，那是上游语义零改动纪律的一部分。

**Consequences**: 额度数据的正确性要求是「够新、够用」而非「实时、精确」；快照 TTL 过期只是降级信号，不阻塞任何请求。
