# 保留上游多用户架构，不简化为单用户

HClaw 实际是个人自托管（admin 单人使用），但保留上游完整的多用户模型（RBAC/ACL/账单骨架）不做简化。单用户化会伤及上游安全边界与账单/配额数据结构，违背「上游语义零改动」的 fork 可持续合流对价；额度感知路由按「每用户余额 × 每供应商额度」双维度设计，多用户本就是它的天然形状。

**Consequences**: 个人使用时所有多用户表照常存在但只有一行 admin 数据——这是预期形态，不是待清理的冗余。
