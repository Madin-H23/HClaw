# HClaw

自托管多渠道 Agent 工作台（miniclaw 深度二开）：在真实额度数据之上做模型供应商选路。一个工作台，两层接入面：IM 渠道进消息，供应商出模型。

## Language

**供应商 (Provider)**:
一个 Anthropic 协议兼容的模型接入端点（上游 UnifiedProvider 概念的延续）。
_Avoid_: 渠道（那是 IM 侧）、厂商（仅指背后公司时可用）

**渠道 (Channel)**:
IM 消息通道：飞书、Telegram、QQ、钉钉、微信、Discord、WhatsApp。
_Avoid_: provider（模型侧一律叫供应商）

**供应商额度**:
某供应商当前的剩余额度与成本数据，由外部额度监控工具（quota-tool）供数。
_Avoid_: 配额（上游 billing 语境专指用户侧）

**用户余额**:
某用户在上游账单体系内的剩余额度，沿用上游 billing，不改其语义。
_Avoid_: 裸用「额度」——与供应商额度歧义，必须带前缀

**额度感知路由 (Quota-Aware Routing)**:
综合供应商剩余额度、模型单价与已花成本、用户余额三类真实数据自动选择供应商的选路策略；是上游健康度/权重选路之上叠加的新维度。数据三源：quota-tool（剩余额度，实时查）、CC Switch 库（单价+已花成本，只读）、上游 billing（用户余额）。
_Avoid_: 智能路由、负载均衡（上游已专指健康度/权重策略）

**额度档位 (Quota Tier)**:
统一四档标尺：充足 / 紧张 / 临界 / 耗尽；各家异构的额度信号（绝对剩余、整数百分比、美元余额）归一化后的共同语言。
_Avoid_: 健康度（上游专指连接健康）

**额度快照 (Quota Snapshot)**:
HClaw 侧缓存的一份供应商额度数据，带数据时间戳；过期后仅作参考，不阻塞选路。
_Avoid_: 缓存（裸用）

**降档 (Downgrade)**:
额度不足时把请求换成更低成本模型档位的行为，只发生在轮边界。
_Avoid_: 切换（中性词含主动换档）、fallback（上游专指失败触发的降级）

**额度否决 (Quota Veto)**:
Agent 显式绑定模型时，额度闸仍能拦截或降档该绑定的机制——绑定不构成绕过额度策略的旁路。
_Avoid_: 拦截（裸用）

**主动消息 (Proactive Message)**:
系统侧不经用户输入发起、推送到指定渠道私聊会话的消息；由定时任务或未来事件触发，受频控约束。
_Avoid_: 推送（泛指）、广播（群语义）

**任务通知渠道 (Task Notify Channels)**:
定时任务 `notify_channels` 字段的声明语义（票 #26 起）：勾选即把该渠道的完成播报收归主动消息入口独占投递（受频控，批二 B4 起），上游旧投递面在这些渠道上让位——旧 fan-out 只保留注册表外声明 id 与绑定渠道（`delivery_route_jid`）投递（agent 任务错误通知、脚本任务完成通知）。一个字段一个消费者（主动消息路径），勿造第二声明面。
_Avoid_: 通知渠道（泛指）、推送目标（指频控配置的渠道默认私聊目标）

**频控 (Rate Control)**:
主动消息的三层节制：渠道全局限速、任务级静默窗口、同会话同类型冷却去重；配置驱动，无 UI。
_Avoid_: 限流（未定语义）、限速（仅指第一层时可用）
